import config from '../../widgetConfig.json';

export const dirServices    = config.urlBase+"/../../../../services/widgets";

export const dirImgs        = config.urlBase+"/.."

export const wConfig = config;

